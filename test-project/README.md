# test-project

This dir contains Sentry's
[main `package.json`](https://github.com/getsentry/sentry/blob/master/package.json),
you can run the `prod-only` script against it to see how it does.

Note that the `pnpm` setting `onlyBuiltDependencies` was removed from the
`package.json`.

See `../package.json` and the development section in `../README.md`.
